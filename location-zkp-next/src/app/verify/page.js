"use client";
import { LocationVerifier } from '@/components/LocationVerifier';

export default function VerifyPage() {
  return <LocationVerifier />;  // Remove all wrappers, just return the component directly
}